% Time form: Present tense

\chapter{Concepts}\label{ch:concepts}

This chapter introduces the core concepts that underpin the implementation and evaluation presented in this thesis.
It begins with the core terminology of the \gls{NSAK} framework inherited from the predecessor project.
It then describes the layered configuration management, followed by the test environment setup.
Finally, it completes the \gls{AI}-related concepts relevant for this work.

\section{Predecessor Project: Project 2}\label{sec:predecessor-project-project-2}

This section summarizes the predecessor project that led to the bachelor's thesis.
The \gls{NSAK} is a cybersecurity framework that operates with the following resources and concepts:
\textit{Devices}, which are physical or virtual machines capable of running the \gls{NSAK}.
The security operation is executed in a network environment and can involve one or more attacks or defense scenarios.
The smallest unit in the framework is a drill, which can be orchestrated in a scenario and chained with other drills.
Before this thesis, \gls{NSAK} already provided a working proof of concept built around the following capabilities:
\begin{enumerate}
    \item The resource types \textit{Environment}, \textit{Drill}, and \textit{Scenario} could be declared as \gls{YAML} resources, and drills could be chained into scenarios and executed manually from the \gls{CLI}. The \textit{Device} type existed only as a concept with provisioning instructions and was not yet implemented in the core.
    \item Scenarios were containerized as \glsxtrshort{OCI} images and executed via Podman.
    \item A containerized \enquote{simple TCP client--server} lab allowed scenarios to be simulated before running them against a physical network.
\end{enumerate}
This was demonstrated with two scenarios: a \gls{MITM} attack (host discovery, \gls{ARP} spoofing, and a transparent \gls{TCP} proxy) and a Rogue Access Point that captured the forwarded traffic.

What the predecessor declared to future work was a flexible configuration management, a more complete cleanup mechanism, broader test coverage, a \gls{REST API}, and a \gls{GUI}.
To integrate \gls{AI} was not part of the framework at all and also not mentioned in future work section.
Modularity and the proof of concept were confirmed.
The remaining open points motivates to conclude with this thesis and the goals are defined in section~\ref{ch:project-goals}.

\subsection{Device}\label{subsec:device-concept}

A \textbf{Device} resource describes a physical or virtual machine that is capable of running the \glsxtrshort{NSAK} framework.
It serves as the execution host for scenarios and drills and is typically deployed at a strategic network position to give it the access required for the operations it performs.

A device exposes its network configuration to the framework by declaring its interfaces and their \gls{IP} addresses in its resource \glsxtrshort{YAML}.
As an alternative, a special device with the id \enquote{Unknown} was added, which auto discovers the hosts network configuration.
This allows scenarios and drills to resolve interface names and target addresses without hardcoding network details, making the same scenario portable across different devices and network environments.

\subsection{Environment}\label{subsec:environment-concept}

An \textbf{Environment} represents a specific network topology, including its infrastructure components, hosts, and services~\cite{nsakRepository2026}.
It describes the target network context in which a scenario is intended to operate.
For example, a simple client--server setup over a switch, a \gls{WLAN} access point with connected clients, or a segmented business network.

Environments are referenced by scenarios to make explicit which network context they are designed for, helping operators select and validate the right configuration before executing an operation.

\subsection{Drill}\label{subsec:drill-concept}

A \textbf{Drill} is a self-contained, reusable unit of execution with a specific, well-defined goal~\cite{nsakRepository2026}.
Its goal can be an active or passive attack step, network discovery, traffic capture, a configuration change, or any other discrete action relevant to a red or blue team activity.

Examples of drills include \gls{ARP} spoofing, host discovery via \gls{ARP} scan, transparent \gls{TCP} proxying, traffic capture with T-Shark, and network configuration tasks such as enabling IP forwarding or \gls{NAT}.

Each drill declares the system and Python packages it requires as well as a typed \mintinline{bash}{interface} that lists its input arguments and return type.
The return value of one drill can be consumed as the input of a subsequent drill within a scenario, enabling data to flow through a chain of execution steps.

\subsection{Scenario}\label{subsec:scenario-concept}

A \textbf{Scenario} describes a concrete red or blue team use case and orchestrates a sequence of drills targeted at one or more environments~\cite{nsakRepository2026}.
Where a drill is a single technical reusable and modular building block, a scenario is an implementation of a specific attack or defense operation.

Like drills, scenarios declare a typed \mintinline{bash}{interface} with input arguments, allowing operators to parameterise a scenario at execution time via the \glsxtrshort{CLI} without modifying the scenario definition itself.
This separation keeps scenarios reusable across different network configurations while still allowing runtime flexibility.


\section{Configuration Management}\label{sec:configuration-management-concepts}

The \glsxtrshort{NSAK} framework distinguishes three configuration layers, each with a different scope and lifetime.
Keeping them separate avoids coupling concerns that change at different rates and makes it easier to reason about where a given piece of configuration comes from.

\subsection{Static Configuration}\label{subsec:static-configuration-concept}

The static configuration layer involves values which are specific for a deployment and are very unlikely to change after the initial provision of the \gls{NSAK} device.
It mainly establishes the filesystem layout that all other parts of the framework depend on.
For example, where the resource library is located, where runtime data is written, and what the base directory of the installation is.

\subsection{Runtime Configuration}\label{subsec:runtime-configuration-concept}

The runtime configuration layer holds mutable state, which might be changed by an operator via the \gls{CLI}.
It is stored as a \glsxtrshort{YAML} file, which is currently loaded at the start of every invocation.
If no configuration file exists yet, a default is created automatically on first access, so no explicit initialization step is required from the user.

\subsection{Resource Configuration}\label{subsec:resource-configuration-concept}
The resource configuration layer is provided by the resource library rather than the framework core.
Each resource declares its own configuration inside a \glsxtrshort{YAML} file stored alongside its implementation.
The framework reads these files to discover and load resources at runtime.

\subsubsection{Typed Interface Arguments}\label{subsubsec:typed-interface-arguments}
Drills and scenarios declare a typed \mintinline{bash}{interface} in their resource configuration \glsxtrshort{YAML} file, that lists the arguments with an explicit type annotation and an optional default value.
The framework reads these annotations at runtime and automatically generates the correct \glsxtrshort{CLI} options for each resource.
This means that adding a new argument to a drill or scenario immediately exposes it as a typed \glsxtrshort{CLI} option without requiring any changes to the framework or the \glsxtrshort{CLI} code.

\subsubsection{Mergeable YAML Structure}\label{subsubsec:mergeable-yaml-structure}
All resource \glsxtrshort{YAML} files follow a common structure in which resources are nested under a resource-type key (e.g.\ \mintinline{bash}{drills:}, \mintinline{bash}{scenarios:}).
This structure ensures that \glsxtrshort{YAML} files from different parts of the library could be merged into a single in-memory data structure without key conflicts, which is a prerequisite for supporting multiple library paths and composite library configurations in the future.

\subsection{Reproducible Test Environment}\label{subsec:reproducible-test-environment-concept}

To ensure a controlled and safe evaluation of the implementation, a simulated test environment is required.
This approach prevents unintended interference with production systems and ensures reproducibility during the development and testing phases.
For this exact purpose the~\enquote{Environment}\ref{subsec:environment-concept} resource type was implemented in the predecessor project~\ref{sec:predecessor-project-project-2}.
Concretely, a simplified network topology comprising a client (Alice) and a server (Bob) is used to emulate and analyze a \gls{MITM} scenario.

While \gls{Docker} and \gls{Podman} provide a general-purpose containerization platform,
they abstract away most of the lower levels of the network stack and thus lack the support for modeling network topologies.
In contrast, \gls{Containerlab} builds upon \gls{Docker Compose} and optimize its functionalities for networking purposes~\cite{containerlab}.

The primary advantages of \gls{Containerlab} are:
\begin{itemize}
    \item It's ability to create a reproducible containerized network topology based on a simple configuration file
    \item Support multiple interfaces nodes which closely resemble real-world network devices (in comparison \gls{Docker} typically operates with a single network interface).
    \item The rapid creation, extension and teardown of complete network environments, ensure that each experiment starts from a clean and controlled state.
\end{itemize}
In summary, while OCI containers serves as the underlying container engine, \gls{Containerlab} provides a higher-level network simulation.

\subsection{Additional \gls{AI} Related Concepts}\label{subsec:additional-ai-related-concepts}

This section will not repeat the concepts already introduced in section \nameref{sec:ai-research}.
\todo{warum braucht es den satz???}
\subsubsection{LangChain}\label{subsubsec:langchain-concept}

LangChain is an open-source Python framework that provides abstractions for building applications powered by \glspl{LLM}~\cite{langchain_repo}.
Building upon these abstractions, LangGraph provides a low-level orchestration framework for implementing stateful and long-running agent workflows.
Unlike LangChain's higher-level agent interfaces, LangGraph represents applications as graphs consisting of nodes and edges, enabling explicit control over execution flow, state management, persistence, and multi-agent coordination~\cite{langgraph_docs}.
On top of LangGraph,
LangChain provides higher-level agent abstractions, standardized interfaces for
models and tools, and pre-built agent architectures.
At an even higher abstraction level, Deep Agents provide a pre-built agent harness built on top of LangGraph.
In addition to the standard agent loop, Deep Agents include capabilities such as task planning, context management through file-system based memory, sub-agent delegation, and long-term memory, making them suitable for complex autonomous workflows~\cite{deepagents_docs}.
In this thesis, LangGraph is used as the primary framework for implementing both single- and multi-agent reconnaissance scenarios.


\subsubsection{Structured Output}\label{subsubsec:structured-output-concept}

By default, \glspl{LLM} produce free-form text responses.
In contrast, structured output allows to generate responses conforming to a predefined schema, such as a \gls{JSON} object with typed fields.
This is typically enforced through grammar-based sampling or constrained decoding,
which restricts the set of valid tokens at each generation step to those that keep the output consistent with the target schema~\cite{langchain_structured_output}.
While most commercial inference \glspl{API} support the described \textit{native} implementation,
it is also possible achieve the same functionality on the agent side with tool calling.
In \gls{LangChain} both methods are supported,
which allows to use setups without native support for structured output~\cite{langchain_structured_output}.
The \gls{AI} based scenarios are leveraging this feature to return Python data structures,
which simplifies the comparison and evaluation of the results.

\subsubsection{Prompt Engineering}\label{subsubsec:prompt-promptengineering-concept}
This term describes the practice of designing and refining the input provided to an \gls{LLM} to improve the quality of the output.
This includes structuring instructions, providing examples, and defining output constraints.
In agentic systems, the system prompt is the primary mechanism through which an agent's behavior, scope, and reasoning style are defined.
Even though we did not set our focus on Prompt Engineering,
we implicitly used it to improve and fine tune the agents behavior during the implementation and evaluation phase of the thesis.

\subsubsection{Reasoning}\label{subsubsec:reasoning-concept}
In the context of \glspl{LLM}, reasoning refers to the model's ability to decompose a problem into intermediate steps and derive a conclusion from them.
This is typically achieved through prompting strategies such as chain-of-thought~\cite{wei2022chain},
where the model is instructed to think step by step before producing a final answer.
\gls{LangChain} leverages reasoning by default for agents with tool calling support,
which is relevant during the implementation and evaluation phase of the thesis.

\subsubsection{\gls{RAG}}\label{subsubsec:rag-concept}
Retrieval Augmented Generation is a technique that extends an \gls{LLM} by retrieving relevant documents or knowledge at inference time and including them in the context window alongside the user prompt~\cite{lewis2020rag}.
This allows the model to ground its responses in external, up-to-date, or domain-specific information without requiring retraining.
We did not implement a \gls{RAG} system in the \gls{AI} based scenarios,
because the evaluation is focused on the pure tool calling capabilities of the current models.

The concepts introduced in this chapter form the foundation for the decisions made throughout the rest of the thesis.
The following chapter defines the methodology, specifying the evaluation setup, scoring criteria, and test environments used to assess the implementation presented in Chapter 5.
